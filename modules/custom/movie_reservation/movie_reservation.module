
<?php


function movie_reservation_theme($existing, $type, $theme, $path)
{
  return array(
    'movie_list' => array(
      'variables' => array('movies' => array(), 
        'title' => '', 
        'genres' => array(), 
        'days' => array())
        
    )
  );
}
